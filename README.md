# Reporte de dólar diario

## Uso rápido
```powershell
# activar entorno
.\.venv\Scripts\Activate.ps1

# ejecutar una sola vez
python .\src\app\main.py

# dejar corriendo y que ejecute cada día a la hora de config.yaml
python .\src\app\main.py --loop
